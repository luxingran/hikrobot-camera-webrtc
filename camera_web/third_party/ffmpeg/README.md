# FFmpeg

FFmpeg binaries are not committed to this repository.

Download FFmpeg for Windows and place the executable here:

```text
camera_web/third_party/ffmpeg/bin/ffmpeg.exe
```

You can also update the application configuration/code to point to an FFmpeg executable installed elsewhere on the system.
