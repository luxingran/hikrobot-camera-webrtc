# MediaMTX

MediaMTX binaries are not committed to this repository.

Download MediaMTX for Windows and place the executable here:

```text
camera_web/third_party/mediamtx/mediamtx.exe
```

The sample `mediamtx.yml` is kept as a reference configuration. Review and adjust it for your deployment environment.
